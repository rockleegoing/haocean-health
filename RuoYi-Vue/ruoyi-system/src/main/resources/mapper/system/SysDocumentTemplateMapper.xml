<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysDocumentTemplateMapper">

    <resultMap type="SysDocumentTemplate" id="SysDocumentTemplateResult">
        <result property="id"             column="id"             />
        <result property="templateCode"   column="template_code"   />
        <result property="templateName"   column="template_name"   />
        <result property="categoryId"     column="category_id"     />
        <result property="sort"           column="sort"             />
        <result property="templateType"   column="template_type"   />
        <result property="category"       column="category"       />
        <result property="filePath"       column="file_path"       />
        <result property="fileUrl"        column="file_url"        />
        <result property="version"        column="version"        />
        <result property="isActive"       column="is_active"       />
        <result property="delFlag"        column="del_flag"        />
        <result property="createBy"       column="create_by"       />
        <result property="createTime"     column="create_time"     />
        <result property="updateBy"       column="update_by"       />
        <result property="updateTime"     column="update_time"     />
        <result property="remark"         column="remark"          />
        <result property="industryCategoryName" column="industry_category_name"/>
    </resultMap>

    <sql id="selectSysDocumentTemplateVo">
        select t.id, t.template_code, t.template_name, t.category_id, t.sort, t.template_type, t.category,
               t.file_path, t.file_url, t.version, t.is_active, t.del_flag, t.create_by, t.create_time,
               t.update_by, t.update_time, t.remark,
               GROUP_CONCAT(DISTINCT ic.category_name ORDER BY ic.category_name SEPARATOR ', ') as industry_category_name
        from sys_document_template t
        left join sys_document_template_industry ti on t.id = ti.template_id
        left join sys_industry_category ic on ti.industry_category_id = ic.category_id
    </sql>

    <select id="selectSysDocumentTemplateList" parameterType="SysDocumentTemplate" resultMap="SysDocumentTemplateResult">
        <include refid="selectSysDocumentTemplateVo"/>
        <where>
            t.del_flag = '0'
            <if test="templateCode != null and templateCode != ''">
                and t.template_code = #{templateCode}
            </if>
            <if test="templateName != null and templateName != ''">
                and t.template_name like concat('%', #{templateName}, '%')
            </if>
            <if test="categoryId != null">
                and t.category_id = #{categoryId}
            </if>
            <if test="templateType != null and templateType != ''">
                and t.template_type = #{templateType}
            </if>
            <if test="category != null and category != ''">
                and t.category = #{category}
            </if>
            <if test="isActive != null and isActive != ''">
                and t.is_active = #{isActive}
            </if>
        </where>
        group by t.id
        order by t.id asc
    </select>

    <select id="selectSysDocumentTemplateById" parameterType="Long" resultMap="SysDocumentTemplateResult">
        <include refid="selectSysDocumentTemplateVo"/>
        where t.id = #{id}
        group by t.id
    </select>

    <select id="selectAllSysDocumentTemplates" resultMap="SysDocumentTemplateResult">
        <include refid="selectSysDocumentTemplateVo"/>
        WHERE t.del_flag = '0' AND t.is_active = '1'
        GROUP BY t.id
        ORDER BY t.category_id ASC, t.sort ASC, t.id ASC
    </select>

    <insert id="insertSysDocumentTemplate" parameterType="SysDocumentTemplate" useGeneratedKeys="true" keyProperty="id">
        insert into sys_document_template
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="templateCode != null and templateCode != ''">template_code,</if>
            <if test="templateName != null and templateName != ''">template_name,</if>
            <if test="templateType != null and templateType != ''">template_type,</if>
            <if test="category != null and category != ''">category,</if>
            <if test="filePath != null">file_path,</if>
            <if test="fileUrl != null">file_url,</if>
            <if test="version != null">version,</if>
            <if test="isActive != null">is_active,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="remark != null">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="templateCode != null and templateCode != ''">#{templateCode},</if>
            <if test="templateName != null and templateName != ''">#{templateName},</if>
            <if test="templateType != null and templateType != ''">#{templateType},</if>
            <if test="category != null and category != ''">#{category},</if>
            <if test="filePath != null">#{filePath},</if>
            <if test="fileUrl != null">#{fileUrl},</if>
            <if test="version != null">#{version},</if>
            <if test="isActive != null">#{isActive},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="remark != null">#{remark},</if>
        </trim>
    </insert>

    <update id="updateSysDocumentTemplate" parameterType="SysDocumentTemplate">
        update sys_document_template
        <trim prefix="SET" suffixOverrides=",">
            <if test="templateCode != null and templateCode != ''">template_code = #{templateCode},</if>
            <if test="templateName != null and templateName != ''">template_name = #{templateName},</if>
            <if test="templateType != null and templateType != ''">template_type = #{templateType},</if>
            <if test="category != null and category != ''">category = #{category},</if>
            <if test="filePath != null">file_path = #{filePath},</if>
            <if test="fileUrl != null">file_url = #{fileUrl},</if>
            <if test="version != null">version = #{version},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteSysDocumentTemplateById" parameterType="Long">
        update sys_document_template set del_flag = '1' where id = #{id}
    </delete>

    <delete id="deleteSysDocumentTemplateByIds" parameterType="String">
        update sys_document_template set del_flag = '1' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectByTemplateIds" parameterType="java.util.List" resultMap="SysDocumentTemplateResult">
        <include refid="selectSysDocumentTemplateVo"/>
        WHERE t.del_flag = '0' AND t.is_active = '1'
          AND t.id IN
        <foreach collection="templateIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        GROUP BY t.id
        ORDER BY t.category_id ASC, t.sort ASC, t.id ASC
    </select>

</mapper>
